/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // Pastikan tidak ada konfigurasi lain yang aneh di sini
};

export default nextConfig;